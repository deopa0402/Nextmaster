import Link from 'next/link';

export default function Home() {
    const name = '홈화면';
    return (
        <div>
            <h1 className="title">으악</h1>
            <p className="title-sub">{name}</p>
        </div>
    );
}
